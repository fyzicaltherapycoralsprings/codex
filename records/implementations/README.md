# Implementations

Add one markdown file per successful customization or fix using `templates/IMPLEMENTATION_RECORD_TEMPLATE.md`.

Recommended naming:
`YYYY-MM-DD-short-feature-name.md`

Record:
- exact commands
- exact config keys and values
- files changed
- rollback steps
- why the change should be remembered
